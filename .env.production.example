# Production environment — copy to .env.production (never commit real values)
# Set these in Vercel / Render / Netlify environment variables dashboard.

# Prefer same-site API host (api.yourdomain.com) so cookies work.
# If the API is on another domain (e.g. Render), the app uses Bearer tokens for Safari/iOS.
VITE_API_URL=https://api.eunikclothings.com/v1
VITE_PAYSTACK_PUBLIC_KEY=pk_live_...
